@model WFH.Models.DayAtHome

@if (ViewBag.AuthenticationID == Model.Account.UserID) {
    using (Html.BeginForm("Delete", "DaysAtHome", FormMethod.Post, new { id = "delete_form", style = "display: inline;" }))
    {
        @Html.HiddenFor(model => model.ID);
       <input type="submit" value="×" class="close" />
    }
}
